@{
    ViewData["Title"] = "User Options";
}

<div class="container mt-5">
    <h2 class="text-center">This is the use choice page</h2>
    <p class="text-center">If you are a user, please choose one of the options below to continue.</p>

    <div class="text-center mt-4">
        <!-- Button to navigate to Login page -->
        <a href="@Url.Action("Login", "Account")" class="btn btn-primary mx-2">Login</a>

        <!-- Button to navigate to Register page -->
        <a href="@Url.Action("Register", "Account")" class="btn btn-outline-secondary mx-2">Register</a>
    </div>
</div>
