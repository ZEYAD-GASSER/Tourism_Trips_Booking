@{
    ViewData["Title"] = "Choose Your Role";
}

<div class="text-center mt-5">
    <h2>choice role</h2>
    <p class="lead">Please choose your role:</p>

    <div class="d-flex justify-content-center gap-3 mt-4">
        <a href="@Url.Action("AdminLogin", "Home")" class="btn btn-primary btn-lg">I am Admin</a>
        <a href="@Url.Action("UserChoice", "Home")" class="btn btn-secondary btn-lg">I am User</a>
    </div>
</div>
